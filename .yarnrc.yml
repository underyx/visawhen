compressionLevel: mixed

enableGlobalCache: false

# Install scripts run only for the packages allowed in package.json's
# dependenciesMeta (sqlite3, which builds its native module); Yarn warns about
# any other package that has one.
enableScripts: false

nodeLinker: pnp

yarnPath: .yarn/releases/yarn-4.18.0.cjs
